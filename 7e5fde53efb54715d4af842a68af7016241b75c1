The linked Discord message is:

> So does rule #4 prevent me from mentioning how absolutely awesome the v2.2.0 experimental MO2 archive conflict detection version is so far?  I've already found a ton of loose files I'm gonna hide so the ones in archive can be operative (for example, 621 loose .wav files in Lucidity FX overwriting the wav's in the AOS archive, when I want the opposite to be true.)
> And, um, wow on script conflicts with the enai mods.  Like Sofia overwriting a Wildcat script.
>
> -- Qwinn in the xEdit Discord server's mod-talk channel at 2018-09-27, 11:35 PM

which is followed by:

> Both those scripts should be deleted.
> They are old versions of a SkyUI script that will mess stuff up if they are the conflict winner.
> Legacy's prepstation and other functions do not work if the script from either of those mods is overwriting the new one from SkyUI SE, for example.
>
> -- SirJesto in the xEdit Discord server's mod-talk channel at 2018-09-27, 11:50 PM
